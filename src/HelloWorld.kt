fun main(){ //The main function will be seen in all Kotlin programs
    println("Hello World")
}

/*
* fun == declares a function
* main() == common function used to execute code
* {} == executes all code between them; starts and ends the function
* println() == nested function that outputs/prints text
* */